# VocabFlow 新增功能说明书

> 基于项目 v1.6 现有功能分析，提出以下功能扩展要求
>
> 分析日期：2026-07-19
> 项目版本：v1.6（静态网页版 + Android v1.0.2）

---
新增功能

### 2.1 学习模式扩展

#### 2.1.1 单词拼写模式（正向听写）

**功能描述**
与现有听写模式不同，此模式显示中文释义，用户需要拼写英文单词。类似句子拼写练习的逐字母输入，但针对单个单词。

**核心交互**
- 显示中文释义和词性
- 用户通过键盘输入英文拼写
- 输入正确自动进入下一词，错误抖动反馈
- 支持提示功能（逐字母揭示）
- SRS 评分持久化，与复习模式共享进度

**实现要点**
- 新增 `/spelling` 路由和 `Spelling` 页面
- 复用 `generateReviewQueue` 生成队列
- 复用 `reviewAndPersist` 持久化 SRS 状态
- 侧边栏新增"拼写"导航项（`wordOnly: true`）


---
#### 2.1.4 配对游戏模式

**功能描述：**
屏幕展示 6-8 个单词和对应释义（打乱排列），用户通过点击将它们配对。配对正确消失，配对错误抖动反馈。

**用户价值：**
- 游戏化学习，增加趣味性
- 适合复习已学单词，巩固记忆
- 支持限时挑战模式

**实现思路：**
- 新增 `/match` 路由和 `MatchGame.tsx` 页面
- 从复习队列取 6-8 个单词，打乱展示
- 点击单词 → 点击释义 → 判断匹配
- 限时模式：60 秒内配对尽可能多
- 统计正确率，正确率影响 SRS 评分

**涉及文件：**
- 新增：`src/pages/MatchGame.tsx`

#### 2.1.2 选择题模式（多选一）

**功能描述**
显示一个单词，从 4 个中文释义中选择正确答案（或反向：显示中文，选英文）。适合快速过词、碎片化学习。

**核心交互**
- 卡片显示英文单词 + 发音按钮
- 下方 4 个释义选项（1 正确 + 3 干扰项）
- 干扰项从同词书随机选取
- 选择后立即反馈对错，2 秒后进入下一题
- SRS 评分映射：答对→Good，答错→Again

**实现要点**
- 新增 `/quiz` 路由和 `Quiz` 页面
- 干扰项生成：从 `getWordsByBook` 随机取 3 个其他释义
- 复用 SRS 评分持久化

---

#### 2.1.3 英汉互译模式

**功能描述**
系统随机显示英文或中文，用户需要输入对应翻译。双向练习，加深记忆。

**核心交互**
- 50% 概率显示英文（用户输入中文），50% 概率显示中文（用户输入英文）
- 中文输入接受部分匹配（包含关键词即可）
- 英文输入大小写不敏感
- 支持 SRS 评分


---


#### 2.2.2 生词本/收藏夹

**功能描述**
用户在词库浏览、复习过程中可将单词加入"生词本"，集中攻克难点词汇。

**核心交互**
- 词库列表和单词详情页新增"收藏"按钮（星标图标）
- 复习页新增"加入生词本"按钮
- 新增 `/favorites` 路由，展示所有收藏单词
- 生词本支持独立复习模式（仅复习收藏的词）
- 数据存储在 localStorage（`vf_favorites`）

**实现要点**
- 新增 `favorites` 工具模块（`src/utils/favorites.ts`）
- `localDb.ts` 新增收藏 CRUD 函数
- 侧边栏新增"生词本"导航项


---

#### 2.2.3 跨词书搜索

**功能描述**
当前搜索仅在当前词书范围内进行。新增全局搜索，可跨所有词书查词。

**实现要点**
- 词库页新增"全局搜索"开关
- `searchWords` 扩展为可接受 `bookId[]` 参数
- 搜索结果显示来源词书标签
- 点击结果可切换到对应词书查看详情


---

### 2.3 复习策略优化

#### 2.3.1 定向复习模式

**功能描述**
用户可选择特定范围进行复习，而非全词书随机。包括：
- 按首字母范围（A-D, E-H...）
- 按学习状态（新词/学习中/已掌握）
- 按错误次数（错误 ≥3 次的词）
- 按上次评分（仅复习"忘记"的词）

**实现要点**
- 复习页新增"筛选"按钮，弹出筛选面板
- `generateReviewQueue` 增加可选 `filter` 参数
- 筛选条件：`{ letterRange?, state?, minLapses?, lastGrade? }`


---

#### 2.3.2 学习计划与目标

**功能描述**
用户可设置每日学习目标（如：每天学 30 个新词 + 复习 50 个），系统跟踪完成进度。

**核心交互**
- 设置页新增"学习目标"卡片
- 可配置：每日新词数、每日复习数、每周学习天数
- Today 页显示目标完成进度条
- 完成目标时显示庆祝动画
- 连续达成目标记录"完美周"

**实现要点**
- `AppSettings` 新增 `dailyGoal` 字段
- Today 页新增目标进度组件
- `localDb.ts` 新增每日目标完成记录




### 2.4 统计与可视化增强

#### 2.4.1 学习热力图

**功能描述**
在统计页新增类似 GitHub 贡献热力图的学习记录可视化，直观展示每日学习强度。

**实现要点**
- 统计页新增热力图组件
- 数据来源：复习日志 + 句子练习记录
- 颜色深浅表示当日学习量
- 支持点击查看某日详情
- 可选时间范围：近 3 个月 / 近 6 个月 / 近 1 年


---

#### 2.4.2 词书掌握度雷达图

**功能描述**
以雷达图展示用户在各方面（拼写、听写、发音、句子）的能力分布。

**实现要点**
- 统计页新增雷达图（Recharts RadarChart）
- 维度：单词认读、单词拼写、听写正确率、发音评分、句子熟练度
- 数据来源：SRS 卡片状态 + 听写记录 + ASR 评分 + 句子练习


---

#### 2.4.3 遗忘曲线预测

**功能描述**
基于 FSRS 算法，展示当前词汇的遗忘曲线预测，帮助用户理解复习时机。

**实现要点**
- 单词详情页新增"遗忘曲线"图表
- 调用 `ts-fsrs` 的 `createEmptyCard` + 评分数据模拟曲线
- X 轴：天数，Y 轴：记忆保留率
- 标注当前时间点和下次复习时间


---

### 2.5 用户体验优化

#### 2.5.1 学习卡片皮肤/主题

**功能描述**
除了浅色/深色模式，新增多种卡片皮肤（如：护眼绿、纸质羊皮卷、极简白等）。

**实现要点**
- 设置页新增"卡片风格"选择
- 通过 CSS 变量实现主题切换
- 5+ 预设皮肤
- `AppSettings` 新增 `cardTheme` 字段


---

#### 2.5.2 手势操作支持

**功能描述**
在移动端/Android 上支持滑动手势操作：
- 左滑：标记"忘记"（Again）
- 右滑：标记"认识"（Good）
- 上滑：翻转卡片
- 下滑：跳过当前词

**实现要点**
- 新增 `useSwipe` Hook
- 复习页和听写页集成手势
- 卡片跟随手指移动的动画反馈

